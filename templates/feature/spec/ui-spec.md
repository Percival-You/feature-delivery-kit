# {feature} — UI 规格（ui-spec）

> 从原型提取的**结构化** UI 契约（非像素）。  
> 保真规则：`spec/prototype-fidelity-rules.md`（TRAP-UID / U1～U6）。  
> 颜色间距等走 `ui-diff-checklist.md` 视觉节。

## 页面索引

| 页面 ID | 路由 | 原型锚点 | 说明 |
|---------|------|----------|------|
| | | | |

---

## 页面：{page-id}

### 控件边界（U1 — 必填）

| 项 | 值 |
|----|-----|
| 页级筛选 | 无 / 列出元素 ID： |
| **禁止多余控件** | 如：不得增加 {某下拉}（UID-EXTRA-CTL） |
| 原型锚点 | `prototype.html` #page-xxx |

### 布局（U3 — 有分栏时必填）

| 约束 | 值 |
|------|-----|
| 布局模式 | 如：KPI 置顶 + 左明细右小结（**本页专用**，勿与其他页混用） |
| 左栏 | |
| 右栏 | |
| UID 敏感 | UID-LAYOUT-CTX：是 / 否 |

### 列表/表格（U2 — 有表时必填）

| 列名 | 展示格式 | 数据来源 API 字段 | 原型锚点 |
|------|----------|-------------------|----------|
| | 如 `6/8` | `progress.submitted/total` | |

### 展示格式（U4 — 有名称/路径类字段时必填）

| 字段 | 格式 | 生成方 | 说明 |
|------|------|--------|------|
| | 如 `父级 / 子级` | API `department_name` | 禁止仅末级名 |

### 元素

| 元素 ID | 类型 | 文案/标签 | 约束 | 原型锚点 |
|---------|------|-----------|------|----------|
| | | | | |

### 交互

| 触发 | 动作 | 预期 |
|------|------|------|
| | | |

---

## 跨页布局差异（U5 — 多页展示同一实体时必填）

| 页面 | 布局摘要 | 与下列页是否相同 |
|------|----------|------------------|
| page-a | 左小结右明细 | — |
| page-b | 左明细右小结 | 与 page-a **不同** |

---

## MECE 自检

- 页面 × 控件边界 × 布局 × 列表 × 元素 × 交互 — 分层互斥 ✅  
- 结构化项已足够 spec-verifier 机械检查；未落入「视觉走人工」甩锅 ✅
