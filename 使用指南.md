# FDP 使用指南

> 面向日常开发的实操手册。安装与移植请看 [INSTALL.md](./INSTALL.md)，包概览请看 [README.md](./README.md)。

---

## 一、5 分钟理解 FDP

FDP（Feature Delivery Pipeline）解决一件事：**做完的功能和需求/原型对不上**。

做法很简单：

1. **开发前**：把需求写成可检查的清单（Spec Lock），你点头后再写代码  
2. **开发前**：检查文档之间有没有矛盾（Analyze）  
3. **开发后**：检查有没有漏做的 AC（Converge）  
4. **UI**：你自己对照原型走查（本 kit 不自动比图）

平时你跟 AI 说话用**口令**即可，AI 会自动读对应 Skill。  
**名词不懂时**：先看 👉 [docs/名词解析.md](./docs/名词解析.md)。

---

## 二、第一次使用（本仓库）

### 2.1 确认已安装

本仓库已执行过安装。若要重装或升级 kit：

```powershell
.\feature-delivery-kit\scripts\install.ps1
```

然后 **重启 Cursor**（让 Hooks / Agents 生效）。

### 2.2 检查文件是否存在

| 路径 | 作用 |
|------|------|
| `.cursor/fdp.config.json` | 当前 feature、文档路径、门禁开关 |
| `.cursor/hooks.json` | Spec Lock + Converge 自动检查 |
| `.cursor/agents/spec-analyzer.md` | 实现前审查（只读） |
| `.cursor/agents/spec-verifier.md` | 实现后审查（只读） |
| `.cursor/skills/feature-delivery-pipeline/` | 总指挥 |

在 Cursor：**Settings → Hooks** 里应能看到 `fdp-spec-lock-gate` 和 `fdp-converge-check`。

### 2.3 配置（改一次，按需再改）

编辑 `.cursor/fdp.config.json`：

```json
{
  "docsRoot": ".kiro/docs/tech",
  "codePathGlobs": ["project/app/**", "project-front/src/**"],
  "activeFeature": "",
  "gate": {
    "specLockOnWrite": true,
    "convergeOnStop": true,
    "allowWriteWithoutActiveFeature": true
  },
  "testCommand": ""
}
```

| 字段 | 你怎么用 |
|------|----------|
| `docsRoot` | feature 文档放哪（如 `.kiro/docs/tech`） |
| `activeFeature` | **正在开发哪个 feature**（如 `work-brief`）；空 = 不拦写代码 |
| `allowWriteWithoutActiveFeature` | `true` = 没设 feature 时正常开发不受影响 |
| `testCommand` | Converge 结束时可选跑测试（例：`cd project-front && pnpm vitest --run`） |

---

## 三、按场景怎么用（复制口令即可）

### 场景 A：从零开始做新功能

```
按 FDP 全流程，feature 名 my-feature，从需求开始
```

AI 会依次：需求澄清 → PRD/方案 → Spec Lock → …  
你只需在 **Spec Lock 清单** 上确认一次。

---

### 场景 B：已有 PRD + 原型，还没有技术方案

```
按 FDP，feature my-feature，from_architecture，出技术方案和 test-cases
```

---

### 场景 C：已有技术方案，准备开发（最常用）

**第 1 步 — 生成 Spec Lock：**

```
feature work-brief，from_spec_lock，生成 Spec Lock
```

AI 会在 `.kiro/docs/tech/work-brief/spec/` 生成：

- `acceptance.md` — 验收标准  
- `ui-spec.md` — UI 结构（有页面时）  
- `rtm.md` — 追溯矩阵  
- `acceptance-checklist.md` — **你要确认的清单**

**第 2 步 — 你审清单**，没问题后把 `acceptance-checklist.md` 顶部改成：

```yaml
---
approved: true
complexity: standard
feature: work-brief
---
```

并回复 AI：**「Spec Lock 确认，可以开发」**

**第 3 步 — 打开门禁**，编辑 `.cursor/fdp.config.json`：

```json
"activeFeature": "work-brief"
```

**第 4 步 — 开发：**

```
feature work-brief，from_development，按 RTM 开发
```

此时 Hook 会拦截：未 `approved` 就不能写 `project/`、`project-front/src/` 下的业务代码。

---

### 场景 D：不确定从哪一步开始

```
feature work-brief，检测产物，告诉我从哪一步开始
```

AI 会扫目录，告诉你例如：「应从 2.5 Spec Lock 开始，因已有 tech-design 但 spec 未 approved」。

---

### 场景 E：开发做完，检查有没有漏

```
feature work-brief，执行 Converge
```

AI 会：

1. 对照 `rtm.md` 找 pending 的 P0 AC  
2. 调用只读 `spec-verifier` 对照 diff  
3. 出 `converge-report.md`  

有遗漏就继续补开发，再执行一次 Converge，直到收敛。

Agent 每轮结束时，**Hook 也可能自动追问**你补 AC（最多 5 轮）。

---

### 场景 F：实现前检查文档是否打架

```
feature work-brief，执行 Analyze
```

在 **Spec Lock 已 approved、写代码之前** 跑。有 P0 就先改文档，不开发。

---

### 场景 G：小改动（加字段、改文案）

```
简单改动：给 xxx 模块加个字段 yyy，走 fast_track
```

不强制完整 Spec Lock；仍会建议 3～5 条轻量 AC。  
可把 `activeFeature` 留空，或 checklist 里标 `complexity: simple`。

---

### 场景 H：UI 与原型对照（结构 P0 + 视觉 P1）

**分工**（详见 `templates/feature/spec/prototype-fidelity-rules.md`）：

| 类型 | 何时固化 | 谁验收 |
|------|----------|--------|
| 筛选器、表格列、双栏方向、展示格式 | **Spec Lock** → ui-spec + P0 AC | Analyze / Converge 机械查 + 走查**复核** |
| 颜色、间距、字体 | 走查清单 | 负责人人工 |

开发 + Converge 通过后：

1. 确认 Spec Lock 已按 **U1～U6** 写入 `spec/ui-spec.md`（非仅「视觉走人工」）
2. 从 `feature-delivery-kit/templates/feature/ui-diff-checklist.md` 生成 `{feature}/ui-diff-checklist.md`
3. 浏览器打开实现 + 原型，先勾 **结构复核**，再勾 **视觉**

**常见漏检**：Converge 只验 AC/单测，**未写入 spec 的原型细节不会被发现** — 故结构项必须在 Spec Lock 固化。

## 四、一个 feature 的文档长什么样

```
.kiro/docs/tech/{feature}/
├── context.md              # 流水线状态、门禁打勾
├── product-guide.md        # 产品说明（可有）
├── prototype.html          # 原型（可有）
├── tech-design.md          # 技术方案
├── test-cases/             # 测试矩阵（含 common-traps.md 陷阱清单，新建 feature 时一并复制）
├── tasks.md                # 任务拆解
├── ui-diff-checklist.md    # 人工 UI 走查（你填）
├── converge-report.md      # Converge 产出
└── spec/
    ├── acceptance.md
    ├── ui-spec.md              # 含控件边界 / 列表列 / 分栏（有 UI 时）
    ├── prototype-fidelity-rules.md  # 从模板复制；或引用 kit 模板
    ├── rtm.md
    └── acceptance-checklist.md   # ★ Spec Lock 开关
```

**新建 feature 文档**（PowerShell）：

```powershell
$feature = "my-feature"
$dest = ".kiro/docs/tech/$feature"
New-Item -ItemType Directory -Force -Path "$dest/spec"
Copy-Item feature-delivery-kit/templates/feature/* $dest -Recurse
```

把模板里的 `{feature}`、`{date}` 替换成实际值。

**边界陷阱**（`test-cases/common-traps.md`）：Q1～Q4 命中须在业务 test-cases 落地场景码（至少 **BSA-MULTI-NEW**）。

**原型保真**（有 `prototype.html` 时）：Spec Lock 须运行 `spec/prototype-fidelity-rules.md` **U1～U6**；Analyze 缺结构化 ui-spec → P0 阻断。

## 五、你需要亲自做的 3 件事（省不得）

| 时机 | 你做什么 | 在哪 |
|------|----------|------|
| 开发前 | 审 Spec Lock 清单，确认 U1～U6 / 结构化 UI 已进 ui-spec，改 `approved: true` | `spec/acceptance-checklist.md` |
| 开发前 | 设当前 feature | `.cursor/fdp.config.json` → `activeFeature` |
| 提测前 | UI 对照原型：**先结构复核、后视觉** | `ui-diff-checklist.md` |

其余（Analyze、Converge、代码 review）可交给 AI + Hook。

---

## 六、门禁与 Hook 会怎样影响你

### Spec Lock 门禁（写代码前）

- **何时拦**：`activeFeature` 已设置 + `complexity` 不是 `simple` + checklist **未** `approved: true`  
- **拦什么**：写入 `codePathGlobs` 下的文件（默认后端 `project/app`、前端 `project-front/src`）  
- **不拦什么**：`spec/`、`test-cases/`、`context.md`、`.cursor/` 等（见 `exemptWritePaths`）  
- **不拦时**：`activeFeature` 为空（默认允许正常非 FDP 开发）

### Converge 检查（每轮 Agent 结束）

- **前置**：`acceptance-checklist.md` frontmatter 必须已是 `approved: true`（G3 通过）；**Spec Lock 未确认时不触发**（避免 RTM 全 pending 被误当成未收敛）
- 若 `rtm.md` 里还有 **P0 pending**，Agent 可能收到自动追问让你补实现
- 配置了 `testCommand` 且 RTM 无 pending 时，会顺带跑测试
- 最多自动循环 **5 次**（`convergeLoopLimit`）

### 临时关闭门禁

```json
"activeFeature": "",
// 或
"gate": { "specLockOnWrite": false, "convergeOnStop": false }
```

改完保存，重启 Cursor 或等 hooks 热加载。

---

## 七、流程总图（对外 6 步 + 关键门禁）

> ★ = 三道关键门禁（**G3** / **G4** / **G7**）。完整 **G1～G8** 见 [名词解析 § 三](./docs/名词解析.md#三门禁关卡编号)。

```
1 需求澄清（建议先 grill 对齐）              [G1]
    ↓
2 PRD + 技术方案                            [G2]
    ↓
★ 2.5 Spec Lock ──── 你确认 approved: true  [G3]
    ↓
3 项目规范
    ↓
★ 2.6 Analyze                               [G4]
    ↓
4 任务拆解 → 5 开发 + 单测 + review          [G5 Hook]
    ↓
★ 5.5 Converge ──── 可循环，RTM 无 P0 pending [G7]
    ↓
5.6 踩坑回流 FDK（可选）
    ↓
6 接口 / 文档 / 提交                         [G8 交付确认]
    ↓
UI 走查（结构 P0 + 视觉 P1）
```

**已有方案时**：从 ★2.5 切入即可（场景 C）。

---

## 八、移植到其他项目

1. 复制整个 `feature-delivery-kit/` 到新项目根目录  
2. `.\feature-delivery-kit\scripts\install.ps1`  
3. 改 `.cursor/fdp.config.json` 里的 `docsRoot`、`codePathGlobs`  
4. 重启 Cursor  

细节：[INSTALL.md](./INSTALL.md)

---

## 九、常见问题

### Q：Hook 没生效？

- 重启 Cursor  
- Settings → Hooks 看是否加载  
- 确认项目根有 `.cursor/hooks.json` 和 `hooks/fdp-*.mjs`  
- 需要 Node.js 18+

### Q：明明 approved 了还拦写代码？

- 检查 `fdp.config.json` 的 `activeFeature` 是否与 checklist 里 `feature` 一致  
- 检查 `approved: true` 在 frontmatter 里拼写是否正确  
- 写的路径是否在 `codePathGlobs` 内

### Q：不想用 FDP 了，只想正常让 AI 写代码

- 把 `activeFeature` 设为 `""`（空字符串）即可

### Q：kit 更新了怎么办？

```powershell
.\feature-delivery-kit\scripts\install.ps1
```

会同步 Skills / Agents / Hooks；**不会覆盖**你改过的 `fdp.config.json`。

### Q：和 project-front 的 hooks 冲突吗？

不冲突。根目录 FDP hooks 管 Spec Lock / Converge；`project-front/.cursor/hooks.json` 管前端测试 review，各管各的。

---

## 十、口令速查表

| 我想… | 对 AI 说 |
|--------|----------|
| 从头做功能 | `按 FDP 全流程 feature {名}，从需求开始` |
| **对齐问透（grill）** | `feature {名}，对齐一下` / `grill` |
| 只有 PRD，要方案 | `feature {名}，from_architecture` |
| 有方案，要清单 | `feature {名}，from_spec_lock，生成 Spec Lock（含 U1～U6 原型保真）` |
| 清单已确认，开发 | `feature {名}，from_development，按 RTM 开发` |
| 不知道卡在哪 | `feature {名}，检测产物，告诉我从哪一步开始` |
| 实现前审文档 | `feature {名}，执行 Analyze` |
| 做完查遗漏（规格轴） | `feature {名}，执行 Converge` |
| 双轴审查 | `feature {名}，双轴审查` |
| 排查硬 bug | `feature {名}，诊断这个 bug` / `fdp diagnose` |
| 换会话继续 | `feature {名}，写 handoff` → 下会话 `读 handoff.md 后继续` |
| 小改动 | `简单改动 …，走 fast_track` |

---

## 十一、相关文件索引

| 文档 | 用途 |
|------|------|
| **本文件** | 日常怎么用 |
| [docs/名词解析.md](./docs/名词解析.md) | **名词白话解释**（Converge、门禁、双轴、context…） |
| [README.md](./README.md) | 包是什么、目录结构 |
| [INSTALL.md](./INSTALL.md) | 安装、移植、卸载 |
| [docs/fdp-enhance-from-matt/README.md](./docs/fdp-enhance-from-matt/README.md) | grill/双轴/诊断/交接增强说明 |
| `templates/feature/spec/prototype-fidelity-rules.md` | TRAP-UID / U1～U6 原型保真规则 |
| `.cursor/AGENTS.md` | 本工作区 Agent 总览 |
| `.cursor/fdp.config.json` | 运行时配置 |

---

**MECE 自检**：场景 A～H 覆盖从零到交付；人工 / 自动门禁分开展示 ✅；UI 拆为结构（spec+机械）与视觉（人工）✅。
